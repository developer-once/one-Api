/**
 * 创建 Services 层代码
 * @param { String } name
 * @param { Object } data
 * @param { Object } dictionary
 * @return { Object } mocks data
 */
const createServices = async(name, data, dictionary) => {
};

module.exports = {
  createServices,
}